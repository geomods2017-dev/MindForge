workflows:
  ios-workflow:
    name: iOS Build
    environment:
      xcode: latest
      ios_signing:
        distribution_type: development
        bundle_identifier: com.tyler.neuroforge
      vars:
        BUNDLE_ID: com.tyler.neuroforge

    scripts:
      - name: Create export options plist
        script: |
          cat > /Users/builder/export_options.plist <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>development</string>
            <key>signingStyle</key>
            <string>automatic</string>
          </dict>
          </plist>
          EOF

      - name: Verify bundle ID in project
        script: |
          grep -n "PRODUCT_BUNDLE_IDENTIFIER" MindForge.xcodeproj/project.pbxproj || true

      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --project "MindForge.xcodeproj" \
            --scheme "MindForge" \
            --export-options-plist "/Users/builder/export_options.plist"

    artifacts:
      - build/ios/ipa/*.ipa
